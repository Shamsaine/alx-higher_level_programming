#include "list.h"

/**
 * check_cycle - checks if a linked list contains a cycle innit
 *
 * @list: pointer to the head node
 *
 * Return: 0 if there is no cycle, 1 if there is a cycle
 */

int check_cycle(listint_t *list)
{


	return (0);
}
